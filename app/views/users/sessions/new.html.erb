<% provide(:title,'Prijava') %>


<body id="login-body">
<div class="container-fluid">
  <div class="row nav-2">
    <a href="/" class="brand"><%=image_tag 'logo_brand.svg', :class => 'brand-logo'%>AMA ETFOS</a>
    <a href="/"><%=image_tag 'x.svg', :class => 'hvr-grow', :id => 'btn-x' %></a>
  </div>
</div>

<section class="login-area">

  <div class="container">
    <div class="row">
      <h3 class="text-center">PRIJAVA</h3>
      <div class="col-xs-10 col-sm-8 col-md-6 col-lg-4 col-xs-offset-1 col-sm-offset-2 col-md-offset-4 col-lg-offset-4">
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= key %> text-center">
             <p><%= value %></p>
          </div>
        <% end %>
          <%= form_for(resource, as: resource_name, url: user_session_path, :html => {:id => "login"}) do |f| %>
          <%= devise_error_messages! %>
          <form id="login">
              <div class="form-group">
                  <%=image_tag 'email.svg', :class => 'form-icon' %>
                  <%= f.label :email %>
                  <%= f.email_field :email, autofocus: true, :class => 'form-control' %>
              </div>
              <div class="form-group">
                  <%=image_tag 'lock.svg', :class => 'form-icon' %>
                  <%= f.label :password %><br />
                  <%= f.password_field :password, autocomplete: "off", :class => 'form-control' %>
              </div>
                <% if devise_mapping.rememberable? -%>
                <div class="checkbox">
                      <label for="check-box">
                      <%= f.check_box :remember_me %>
                      Zapamti me.</label>
                </div>
                <% end %>
              <%= f.submit "PRIJAVA", :class => 'btn btn-prijava hvr-grow' %>
          <% end %>
          
          <div class="row text-center">
            <div class="btn-group">
              <button class="btn btn-soc-left" id="btn-1-1"><%= image_tag 'facebook_3.svg', :class => 'img-btn' %></button>
              <%= link_to "Facebook prijava", user_omniauth_authorize_path(:facebook), :class => 'btn btn-soc-right', :id => 'btn-1-2' %>
            </div>
          </div>
           
    </div>

  </div>
</div>
</section>

-->
